using UnityEngine;

namespace MADesign {
    public class DemoLoadingSceneController : MonoBehaviour
    {

        // Start is called before the first frame update
        void Start()
        {
            MADesign.YourGameTrackingFunctions.LoadingScene_trackingStart();
        }
    }
}
